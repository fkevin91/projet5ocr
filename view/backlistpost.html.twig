{% extends 'back.html.twig' %}
{% block title %} Liste de mes articles {% endblock %}
{% block body %}

<div>
    <h3 class="p-3">Mes Articles Publiés</h3>
</div>

<div class="container">
    <div class="row g-4">
        {% for infoPost in listpost %}
            <a class="text-decoration-none text-dark col-6" href="homeback?back=homebackUpdate&idblogpost={{infoPost.get_id}}">
                <div class="container p-1 border shadow rounded" style="background-color: #C2C4CC;">
                    <div class="well">
                        <div class="media d-flex">
                            <div class="p-3">
                            <img class="media-object" src="{{infoPost.get_photo_url}}" width="150px" height="150px">
                            </div>
                            <div class="media-body w-50">
                                <h4 class="media-heading"> {{infoPost.get_titre}} </h4>
                                <!-- contenu hidden
                                <p>{{infoPost.get_contenu}}</p>
                                -->
                                <p>{{infoPost.get_date_creation}}</p> 
                                <p>Autheur n° {{infoPost.get_auteur}} | x commentaires</p>
                            </div>
                        </div>
                    </div>
                </div>
            </a>
        {% endfor %}
    </div>
</div>


{% endblock %}
{% block javascript %}
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.0.0-beta3/dist/js/bootstrap.bundle.min.js" integrity="sha384-JEW9xMcG8R+pH31jmWH6WWP0WintQrMb4s7ZOdauHnUtxwoG2vI5DkLtS3qm9Ekf" crossorigin="anonymous"></script>
{% endblock %}